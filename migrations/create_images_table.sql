CREATE TABLE IF NOT EXISTS images (
                                      id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
                                      name TEXT NOT NULL,
                                      path TEXT NOT NULL,
                                      web_link TEXT
);
